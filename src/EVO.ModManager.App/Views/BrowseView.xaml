<UserControl x:Class="EVO.ModManager.App.Views.BrowseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:EVO.ModManager.App.Converters">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolVis"/>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1A1A3E"/>
            <Setter Property="Foreground" Value="#E0E0F0"/>
            <Setter Property="BorderBrush" Value="#2A2A5A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="Transparent" BorderThickness="0">
                                <ToggleButton.Template>
                                    <ControlTemplate><ContentPresenter/></ControlTemplate>
                                </ToggleButton.Template>
                                <Grid>
                                    <Border x:Name="Border" Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4"/>
                                    <Grid Margin="{TemplateBinding Padding}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ContentPresenter Grid.Column="0"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                          VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" Text="▼" FontSize="8"
                                                   Foreground="#6666AA" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    </Grid>
                                </Grid>
                            </ToggleButton>
                            <Popup StaysOpen="False" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding ElementName=ToggleButton}">
                                <Border Background="#1A1A3E" BorderBrush="#2A2A5A"
                                        BorderThickness="1" CornerRadius="4" MaxHeight="300">
                                    <ScrollViewer><ItemsPresenter Margin="0,4"/></ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E0E0F0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#2A2A5A"/></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#3A3A7A"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#161640" BorderBrush="#1E1E4A"
                BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,16">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                            <TextBlock Text="Source:" FontSize="13" Foreground="#6666AA"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox ItemsSource="{Binding Sources}" SelectedItem="{Binding SelectedSource}"
                                      DisplayMemberPath="Name" Width="160" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Category:" FontSize="13" Foreground="#6666AA"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}"
                                      Width="140" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Command="{Binding FetchModsCommand}" Content="Refresh"
                                Background="#1A1A3E" Foreground="#C0C0E0" BorderBrush="#2A2A5A"
                                BorderThickness="1" Padding="14,7" FontSize="13" Cursor="Hand"/>
                        <Border Background="#1A1A3E" CornerRadius="4" Padding="12,5" VerticalAlignment="Center"
                                Margin="10,0,0,0" Visibility="{Binding IsLoading, Converter={StaticResource BoolVis}}">
                            <TextBlock Text="Loading..." Foreground="#6666AA" FontSize="13"/>
                        </Border>
                    </StackPanel>
                </Grid>

                <Border Background="#1A1A3E" CornerRadius="4" Padding="14,8" Margin="0,10,0,0"
                        Visibility="{Binding IsDownloading, Converter={StaticResource BoolVis}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ProgressBar Value="{Binding DownloadProgress}" Height="16"
                                     Foreground="#2ECC71" Background="#0F0F23"
                                     BorderBrush="#1E1E4A" BorderThickness="1" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding StatusText}" FontSize="13"
                                   Foreground="#6666AA" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300" MinWidth="260"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#161640" BorderBrush="#1E1E4A"
                    BorderThickness="1" CornerRadius="8">
                <ListBox ItemsSource="{Binding Mods}" SelectedItem="{Binding SelectedMod}"
                         Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="14,10" CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/><ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" Foreground="#E0E0F0"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="{Binding Author, TargetNullValue='Unknown'}" FontSize="12" Foreground="#6666AA"/>
                                            <TextBlock Text=" · " FontSize="12" Foreground="#5555AA"/>
                                            <TextBlock Text="{Binding Published, StringFormat={}{0:yyyy-MM-dd}}" FontSize="12" Foreground="#6666AA"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <Border Grid.Column="1" Background="#1A1A3E" CornerRadius="4"
                                            Padding="6,3" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Category, TargetNullValue='Mod'}" FontSize="11" Foreground="#6666AA"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Border Grid.Column="1" Margin="16,0,0,0" Background="#161640" BorderBrush="#1E1E4A"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Preview" FontSize="10" Foreground="#5555AA" FontWeight="Bold"/>
                    <Border Margin="0,10,0,0" Background="#1A1A44" BorderBrush="#1E1E4A"
                            BorderThickness="1" CornerRadius="6" Padding="14" MinHeight="200"
                            DataContext="{Binding SelectedMod}">
                        <StackPanel>
                            <TextBlock Text="{Binding Title, TargetNullValue='Select a mod'}" FontSize="15"
                                       FontWeight="Bold" Foreground="#E0E0F0" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding Description, TargetNullValue=''}" FontSize="12"
                                       Foreground="#6666AA" TextWrapping="Wrap" MaxHeight="150" Margin="0,8,0,0"/>
                            <Separator Background="#1E1E4A" Margin="0,10,0,10" Opacity="0.5"/>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="65"/><ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Author:" FontSize="12" Foreground="#6666AA"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Author}" FontSize="12" Foreground="#C0C0E0"/>
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Date:" FontSize="12" Foreground="#6666AA" Margin="0,6,0,0"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Published, StringFormat={}{0:yyyy-MM-dd}}" FontSize="12" Foreground="#C0C0E0" Margin="0,6,0,0"/>
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Type:" FontSize="12" Foreground="#6666AA" Margin="0,6,0,0"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Category}" FontSize="12" Foreground="#C0C0E0" Margin="0,6,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Button Command="{Binding DataContext.DownloadSelectedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Background="#E94560" Foreground="White" BorderThickness="0"
                            Padding="14,9" FontSize="13" FontWeight="SemiBold" Cursor="Hand"
                            HorizontalAlignment="Stretch" Margin="0,12,0,0" Content="Download &amp; Install"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
